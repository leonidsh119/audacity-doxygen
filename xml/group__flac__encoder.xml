<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.11">
  <compounddef id="group__flac__encoder" kind="group">
    <compoundname>flac_encoder</compoundname>
    <title>FLAC/_encoder.h: encoder interfaces</title>
    <innergroup refid="group__flac__stream__encoder">FLAC/stream_encoder.h: stream encoder interface</innergroup>
    <briefdescription>
<para>This module describes the encoder layers provided by libFLAC. </para>    </briefdescription>
    <detaileddescription>
<para>The stream encoder can be used to encode complete streams either to the client via callbacks, or directly to a file, depending on how it is initialized. When encoding via callbacks, the client provides a write callback which will be called whenever <ref refid="namespace_f_l_a_c" kindref="compound">FLAC</ref> data is ready to be written. If the client also supplies a seek callback, the encoder will also automatically handle the writing back of metadata discovered while encoding, like stream info, seek points offsets, etc. When encoding to a file, the client needs only supply a filename or open <computeroutput>FILE*</computeroutput> and an optional progress callback for periodic notification of progress; the write and seek callbacks are supplied internally. For more info see the <ref refid="group__flac__stream__encoder" kindref="compound">stream encoder </ref> module. </para>    </detaileddescription>
  </compounddef>
</doxygen>
