<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.11">
  <compounddef id="class_filter" kind="class" language="C++" prot="public">
    <compoundname>Filter</compoundname>
    <includes refid="libnyquist_2nyquist_2nyqstk_2include_2_filter_8h" local="no">Filter.h</includes>
    <briefdescription>
<para>STK filter class. </para>    </briefdescription>
    <detaileddescription>
<para>This class implements a generic structure that can be used to create a wide range of filters. It can function independently or be subclassed to provide more specific controls based on a particular filter type.</para><para>In particular, this class implements the standard difference equation:</para><para>a[0]*y[n] = b[0]*x[n] + ... + b[nb]*x[n-nb] - a[1]*y[n-1] - ... - a[na]*y[n-na]</para><para>If a[0] is not equal to 1, the filter coeffcients are normalized by a[0].</para><para>The <emphasis>gain</emphasis> parameter is applied at the filter input and does not affect the coefficient values. The default gain value is 1.0. This structure results in one extra multiply per computed sample, but allows easy control of the overall filter gain.</para><para>by Perry R. Cook and Gary P. Scavone, 1995 - 2005.</para><para>This class implements a generic structure that can be used to create a wide range of filters. It can function independently or be subclassed to provide more specific controls based on a particular filter type.</para><para>In particular, this class implements the standard difference equation:</para><para>a[0]*y[n] = b[0]*x[n] + ... + b[nb]*x[n-nb] - a[1]*y[n-1] - ... - a[na]*y[n-na]</para><para>If a[0] is not equal to 1, the filter coefficients are normalized by a[0].</para><para>The <emphasis>gain</emphasis> parameter is applied at the filter input and does not affect the coefficient values. The default gain value is 1.0. This structure results in one extra multiply per computed sample, but allows easy control of the overall filter gain.</para><para>by Perry R. Cook and Gary P. Scavone, 1995 - 2005. </para>    </detaileddescription>
    <location file="C:/_dev/OpenU/Audacity/audacity/lib-src/libnyquist/nyquist/nyqstk/include/Filter.h" line="2" column="1"/>
    <listofallmembers>
    </listofallmembers>
  </compounddef>
</doxygen>
